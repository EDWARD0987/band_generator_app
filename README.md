Let's turn this band name generator into a simple web app using Flask, a lightweight Python framework.

Step 1: Set Up a Flask App, First, install Flask if you haven’t already:

pip install flask



Step 2: Create a directory for your project and cd into directory

Step 3: Create a new file => app.py

Step 4: Create a simple web page 
Inside the same directory, create a folder called templates and add index.html inside it.

Running the Web App = After saving the files, launch the Flask app (make sure you are in the main folder )

python app.py

Step 6: Then, visit http://127.0.0.1:5000/ in your browser to try it out!

You should be able to access the app